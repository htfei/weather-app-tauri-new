[package]
name = "weather-app"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
reqwest = { version = "0.12", features = ["json", "rustls-tls"] }
openssl-src = { version = "300.6", features = ["vendored"] }

[build-dependencies]
tauri-build = { version = "2", features = [] }
