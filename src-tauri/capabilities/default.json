{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for WebWrapper",
  "windows": ["main"],
  "permissions": [
    "core:webview:default",
    "core:window:default",
    "core:app:default"
  ]
}
